import { Injectable } from '@nestjs/common';

/**
 * SyncService (Stub)
 * ──────────────────
 * This is a stub file created during Step 2 (project structure).
 * Full implementation will be added in Step 7.
 *
 * Will provide:
 *   - handleJiraEvent(eventType, payload)          → Sync Jira → Freshservice
 *   - handleFreshserviceEvent(eventType, payload)  → Sync Freshservice → Jira
 */
@Injectable()
export class SyncService {
  // Full implementations coming in Step 7
}
